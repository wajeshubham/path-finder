import { CellInterface } from "../../interfaces";

export const generateRecursiveMaze = (grid: CellInterface[][]) => {
  for (let rowIndex = 0; rowIndex < grid.length; rowIndex++) {
    for (let colIndex = 0; colIndex < grid[0].length; colIndex++) {}
  }
};
